# 🚆 TrackSafe – Development Todo List (v1.1 Edition)

This todo list covers the development phases for the TrackSafe application, extending the MVP to include the v1.1 WebSocket server for lower latency location sharing.

## Phase 1: Project Setup & Core Configuration
- [x] Initialize the Flutter mobile application in the `mobile` directory.
- [x] Set up the directory structure as specified in the PRD (`mobile/lib`, `firebase`, `docs`).
- [x] Add necessary dependencies to `pubspec.yaml`.
- [x] Create basic project documentation (`README.md`, `docs/ARCHITECTURE.md`).
- [x] Create and configure Firebase project.
- [x] Generate and integrate `google-services.json` (Android) and/or `GoogleService-Info.plist` (iOS) into the Flutter project.
- [x] Enable Anonymous Authentication in Firebase Console.
- [x] Initialize Firebase within the Flutter app (`main.dart`).

## Phase 2: Firebase Security & Data Modeling
- [x] Define Firestore security rules in `firebase/rules.firestore` to restrict read/write access.
- [x] Create Firestore indexes in `firebase/indexes.json` if necessary for querying users by location.
- [x] Create data models in Flutter to map Firestore documents.

## Phase 3: Core Features - Live Location Sharing
- [x] Implement UI for requesting location permissions from the user.
- [x] Implement location tracking using the `geolocator` package.
- [x] Create a background timer/stream to update the user's location (GPS data) every 2-5 seconds.
- [x] Write logic to update the current user's location data in the Firestore `users` collection.
- [x] Implement a realtime listener (snapshot) to fetch nearby users' locations from the `users` collection.
- [x] Integrate `google_maps_flutter` to display a map.
- [x] Render the current user's location and nearby users' locations as markers on the map in real-time.

## Phase 4: Core Features - Simple Collision Detection (Client-Side)
- [x] Implement a utility function to calculate the distance between two GPS coordinates (e.g., using `geolocator` distance between).
- [x] Implement a utility function to calculate relative speed between the current user and nearby users.
- [x] Build the collision detection loop.
- [x] Create a trigger function to create an alert of type `collision` when the condition is met.

## Phase 5: Core Features - Emergency Alerts
- [x] Design and implement the UI for the manual Emergency Alert Button ("ADA MASALAH DI REL").
- [x] Implement the logic to create an alert of type `manual` and write it to the Firestore `alerts` collection.
- [x] Implement an active listener on the `alerts` collection to listen for new alerts generated by the current user or others nearby.

## Phase 6: Core Features - Push Notifications (FCM)
- [x] Request push notification permissions from the user.
- [x] Retrieve and store the device's FCM token (optional, depending on the broadcast approach).
- [x] Configure FCM background and foreground message handlers in the Flutter app.
- [x] Implement client-side topic subscription or Cloud Messaging integration so devices receive push notifications when an alert is created.
- [x] Display incoming alerts using local notifications or in-app dialogs/snackbars.

## Phase 7: UI/UX Polish
- [x] Finalize the Main Map Screen layout and styling.
- [x] Design clear and distinct map markers for the current user, other users, and alert locations.
- [x] Implement visual indicators (e.g., red screen flash, persistent banner) when an active alert is triggered.
- [x] Add a visual toggle or setting to disable location broadcasting (privacy control).

## Phase 8: v1.1 - WebSocket Server (Lower Latency)
- [x] Upgrade Flutter SDK to the latest version in `pubspec.yaml`.
- [x] Initialize a simple WebSocket server (e.g., Node.js with `ws` package) in a new `websocket_server` directory.
- [x] Implement broadcast logic on the WebSocket server to stream location updates instantly to all connected clients.
- [x] Add `web_socket_channel` dependency to the Flutter application.
- [x] Refactor `LocationService` in Flutter to push and receive high-frequency GPS updates via WebSocket instead of Firestore for real-time tracking.
- [x] Fallback to Firestore for persistent alert logging while using WebSockets purely for live locations.
